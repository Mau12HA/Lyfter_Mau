
"""
1. Experimente haciendo sumas entre distintos tipos de datos y apunte los resultados.
    1. Si le salen errores, **no se asuste.** Lealos e intente comprender qué significan.
    *Los errores son oportunidades de aprendizaje.*
    2. Por ejemplo:
        1. string + string → 37
        2. string + int → TypeError: can only concatenate str (not "int") to st
        3. int + string → TypeError: unsupported operand type(s) for +: 'int' and 'str'
        4. list + list → TypeError: 'int' object is not iterable
        5. string + list → TypeError: 'int' object is not iterable
        6. float + int → 10.0
        7. bool + bool → TypeError: 'bool' object is not callable
"""

a = 3
b = 7

print(a+b)